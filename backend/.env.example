# ======================================
# BACKEND ENVIRONMENT CONFIGURATION
# ======================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to Git!

# Server Configuration
PORT=4000
NODE_ENV=development

# MongoDB Database
# Get your connection string from MongoDB Atlas (https://www.mongodb.com/cloud/atlas)
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/videosafe?retryWrites=true&w=majority

# JWT Authentication
# Generate a random secret key (use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_random

# Sightengine AI API (Content Moderation)
# Sign up at: https://sightengine.com/signup
# Free tier: 2,000 API calls/month (~333 videos)
# Get your credentials from: https://sightengine.com/dashboard
SIGHTENGINE_USER=your_sightengine_user_id
SIGHTENGINE_SECRET=your_sightengine_secret_key

# File Upload Configuration
UPLOAD_DIR=./uploads

# Admin Account (will be auto-created on first run)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeThisToASecurePassword123!

# Frontend Origins (CORS)
# Add your frontend URLs separated by commas
# For production, add your deployed frontend URL
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:3000

# ======================================
# DEPLOYMENT NOTES:
# ======================================
# For production deployment:
# 1. Set NODE_ENV=production
# 2. Use strong, unique JWT_SECRET
# 3. Use MongoDB Atlas connection string
# 4. Set FRONTEND_ORIGINS to your deployed URL
# 5. Ensure FFmpeg is installed on server
# 6. Configure proper CORS and SSL
